# O frontend fala com o backend via proxy configurado em vite.config.ts durante
# o desenvolvimento (/api -> http://localhost:4000). Em produção, sirva o
# frontend estático atrás do mesmo domínio/proxy do backend, ou ajuste
# vite.config.ts / seu servidor web para apontar /api para a URL do backend.
